import React from 'react';

import VideocamIcon from '@material-ui/icons/Videocam';

const CameraWidget = () => {
  return <VideocamIcon style={{ color: 'orange' }} fontSize='inherit' />;
};

export { CameraWidget };
