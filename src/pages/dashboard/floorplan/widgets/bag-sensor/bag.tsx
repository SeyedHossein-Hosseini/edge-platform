import React from 'react';

import LocalMallIcon from '@material-ui/icons/LocalMall';

const BagWidget = () => {
  return <LocalMallIcon style={{ color: 'darkgreen' }} fontSize='inherit' />;
};

export { BagWidget };
